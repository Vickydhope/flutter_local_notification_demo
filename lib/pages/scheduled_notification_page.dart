import 'package:flutter/material.dart';

class ScheduledNotificationPage extends StatelessWidget {
  const ScheduledNotificationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Scheduled Notification Page"),
      ),
    );
  }
}
